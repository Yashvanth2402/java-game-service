# ============================================================
# GENERATED BY CI/CD PIPELINE AGENT
# ============================================================
# generated      : 2026-03-21 04:37:29 UTC
# source         : LLM-generated (Gemini / gemini-2.5-flash) + Python enforcer
# repo           : Yashvanth2402/java-game-service
# language       : java
# framework      : spring-boot
# build tool     : maven
# version        : 17
# ci tests       : unit, smoke (via platform-deploy.yml)
# helm envs      : dev, test, staging, prod
# deploy branches: dev
# scan branch    : dev
# main branch    : archive only (NOT a deploy branch)
# ============================================================

# main branch intentionally excluded — archive only.
# dev -> DEV env  |  release/* -> STAGING -> PROD

name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed
    branches:
      - dev
      - "release/*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: Yashvanth2402/platform-pipelines/.github/workflows/platform-deploy.yml@main
    with:
      image_tag: ${{ github.event.workflow_run.head_sha }}
      repo_name: ${{ github.event.repository.name }}
      environment: >-
        ${{
          startsWith(github.event.workflow_run.head_branch, 'release/') && 'staging' ||
          'dev'
        }}
    secrets: inherit

  notify-on-failure:
    if: failure()
    needs: [deploy]
    runs-on: ubuntu-latest
    steps:
      - uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: '{"text": "Pipeline failed: ${{ github.repository }}"}'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
