# ============================================================
# GENERATED BY CI/CD PIPELINE AGENT
# ============================================================
# generated      : 2026-03-19 15:08:29 UTC
# source     : LLM-generated (Gemini / gemini-2.5-flash)
# quality    : Tailored to this repo
# repo           : Yashvanth2402/java-game-service
# language       : java
# framework      : spring-boot
# build tool     : maven
# version        : 17
# ci tests       : unit, smoke (via platform-deploy.yml)
# helm envs      : dev, test, staging, prod
# deploy branches: dev
# main branch    : archive only (NOT a deploy branch)
# ============================================================

# NOTE: main branch intentionally excluded — archive only.
# dev → DEV env, release/* → STAGING → PROD
# Smoke tests run automatically in platform-deploy.yml after deploy:
#   - GET /actuator/health (Spring Boot) or /health → expects 200
# Failure blocks promotion to next environment.

name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches:
      - dev
      - "release/*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: Yashvanth2402/platform-pipelines/.github/workflows/platform-deploy.yml@main
    with:
      image_tag: ${{ github.event.workflow_run.head_sha }}
      repo_name: ${{ github.event.repository.name }}
      environment: >-
        ${{
          startsWith(github.event.workflow_run.head_branch, 'release/') && 'staging' ||
          'dev'
        }}
    secrets: inherit

  notify-on-failure:
    if: failure()
    needs: [deploy]
    runs-on: ubuntu-latest
    steps:
      - uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {"text": "Deploy failed: ${{ github.repository }}"}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}