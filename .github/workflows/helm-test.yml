# ============================================================
# GENERATED BY CI/CD PIPELINE AGENT
# ============================================================
# generated      : 2026-03-20 14:13:29 UTC
# source         : LLM-generated (Gemini / gemini-2.5-flash) + Python enforcer
# repo           : Yashvanth2402/java-game-service
# language       : java
# framework      : spring-boot
# build tool     : maven
# version        : 17
# ci tests       : unit, smoke (via platform-deploy.yml)
# helm envs      : dev, test, staging, prod
# deploy branches: dev
# main branch    : archive only (NOT a deploy branch)
# ============================================================

name: Helm Chart Test
'on':
  push:
    branches:
    - dev
    - release/*
    paths:
    - helm/**
  pull_request:
    branches:
    - dev
    - release/*
    paths:
    - helm/**
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Setup Helm
      uses: azure/setup-helm@v4.2.0
      with:
        version: v3.14.0
    - name: Install Kubeval
      run: 'curl -LO https://github.com/instrumenta/kubeval/releases/latest/download/kubeval-linux-amd64.tar.gz

        tar xf kubeval-linux-amd64.tar.gz

        sudo mv kubeval /usr/local/bin/

        '
    - name: Lint Helm chart
      run: helm lint ./helm
    - name: Validate Helm chart with Kubeval
      run: helm template java-game-service ./helm | kubeval --strict
    - name: Helm Dry Run
      run: helm install java-game-service ./helm --dry-run --debug
  notify-on-failure:
    if: failure()
    runs-on: ubuntu-latest
    steps:
    - name: Send Slack Notification
      uses: slackapi/slack-github-action@v1.24.0
      with:
        payload: '{"text": "Helm Test Pipeline failed for ${{ github.repository }} on branch ${{ github.ref_name }}. See details:
          ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"}

          '
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
